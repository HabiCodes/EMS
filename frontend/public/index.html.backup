<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EntryMySlot — Book Sports Venues, Events & Movies</title>
    <meta name="description" content="Book sports turfs, events, and movie tickets near you. EntryMySlot — your one-stop entertainment booking platform.">
    <link rel="canonical" href="https://entrymyslot.com/">

    <!-- Open Graph -->
    <meta property="og:title" content="EntryMySlot — Book Sports Venues, Events & Movies">
    <meta property="og:description" content="Book sports turfs, events, and movie tickets near you.">
    <meta property="og:image" content="https://entrymyslot.com/assets/images/logo.png">
    <meta property="og:url" content="https://entrymyslot.com/">
    <meta property="og:type" content="website">

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="EntryMySlot — Book Sports Venues, Events & Movies">
    <meta name="twitter:description" content="Book sports turfs, events, and movie tickets near you.">

    <!-- Fonts & Icons -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

<<<<<<< HEAD
    <script src="js/config.js"></script>
    <script src="js/api.js"></script>
    <script src="js/customer-turf.js"></script>
    <script src="js/auth.js"></script>
    
=======
>>>>>>> d3846f2 (chore: finalize production frontend)
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: { sans: ['"Plus Jakarta Sans"', 'sans-serif'] },
                    colors: {
                        custom: { light: '#FA580B', dark: '#0126A5', darkHover: '#1a1a1a', lightHover: '#E04A00' }
                    },
                    boxShadow: {
                        'soft': '0 4px 20px -2px rgba(0, 0, 0, 0.05)',
                        'glow': '0 0 15px rgba(250, 88, 11, 0.3)',
                    }
                }
            }
        };
    </script>

    <!-- Production CSS -->
    <style>
        .hide-scrollbar::-webkit-scrollbar { display: none; }
        .hide-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }
        .card-zoom-image { transition: transform 0.5s cubic-bezier(0.4, 0, 0.2, 1); }
        .card-zoom:hover .card-zoom-image { transform: scale(1.08); }
        ::selection { background-color: #FA580B; color: #ffffff; }
        html { scroll-behavior: smooth; }
        .view-section { display: none; }
        .view-section.active { display: block; }
        .payment-radio:checked + div > .check-icon { opacity: 1; transform: scale(1); }
        .payment-option-content { display: none; }
        .payment-radio:checked ~ .payment-option-content { display: block; }
        .ticket-divider {
            background-image: linear-gradient(to right, #e5e7eb 50%, rgba(255,255,255,0) 0%);
            background-position: bottom; background-size: 10px 1px; background-repeat: repeat-x;
        }
    </style>

    <!-- Core scripts (loaded in dependency order) -->
    <script src="js/config.js"></script>
    <script src="js/api/client.js"></script>
    <script src="js/api/mappers.js"></script>
    <script src="js/auth/customer.js"></script>
    <script src="js/auth/organizer.js"></script>
    <script src="js/auth/admin.js"></script>
    <script src="js/location-state.js"></script>
    <script src="js/utils/money.js"></script>
    <script src="js/utils/format.js"></script>
    <script src="js/utils/dom.js"></script>
    <script src="js/components/common.js"></script>
    <script src="js/router.js"></script>
    <script src="js/api/endpoints/movies.js"></script>
    <script src="js/api/endpoints/events.js"></script>
    <script src="js/api/endpoints/turfs.js"></script>
    <script src="js/api/endpoints/bookings.js"></script>
    <script src="js/pages/customer.js"></script>
    <script src="js/pages/admin.js"></script>
    <script src="js/pages/partner.js"></script>
    <script src="js/routes.js"></script>
    <script src="js/main.js"></script>
</head>

<body class="bg-[#FCFCFD] font-sans text-gray-900 antialiased selection:bg-custom-light selection:text-white">

<!-- ==================== SPLASH SCREEN ==================== -->
<div id="splashScreen" class="fixed inset-0 z-[9999] bg-custom-dark flex flex-col items-center justify-center transition-opacity duration-700">
    <img src="assets/images/logo.png" alt="EntryMySlot" class="h-14 sm:h-20 md:h-24 w-auto object-contain">
</div>

<!-- ==================== AUTH OVERLAY ==================== -->
<div id="authOverlay" class="view-section active fixed inset-0 z-[300] bg-custom-dark/70 backdrop-blur-md overflow-y-auto">
    <div class="min-h-screen flex items-center justify-center px-4 py-10">
        <div class="w-full max-w-md">
            <!-- Logo -->
            <div class="text-center mb-8 flex flex-col items-center">
                <img src="assets/images/logo.png" alt="EntryMySlot" class="h-16 sm:h-20 w-auto object-contain mb-2">
                <p class="text-gray-300 text-sm font-medium mt-2" id="authSubtitle">Book Sports Venues & Turfs Near You</p>
            </div>

            <!-- Login Form -->
            <div id="loginFormCard" class="bg-white rounded-3xl shadow-2xl p-8 border border-gray-100">
                <div class="text-center mb-6">
                    <h2 class="text-xl font-extrabold text-gray-900">Welcome Back</h2>
                    <p class="text-sm text-gray-500 font-medium mt-1">Sign in to your account</p>
                </div>
                <div id="loginAlert" class="hidden mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border"></div>
                <div class="space-y-4">
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Email Address</label>
                        <div class="relative">
                            <i class="fa-solid fa-envelope absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                            <input type="email" id="loginEmail" placeholder="you@example.com" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light focus:ring-1 focus:ring-custom-light text-sm font-bold bg-gray-50 transition-all" autocomplete="email">
                        </div>
                    </div>
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Password</label>
                        <div class="relative">
                            <i class="fa-solid fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                            <input type="password" id="loginPassword" placeholder="Enter your password" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light focus:ring-1 focus:ring-custom-light text-sm font-bold bg-gray-50 transition-all" autocomplete="current-password">
                        </div>
                    </div>
                    <button onclick="handleCustomerLogin()" id="loginBtn" class="w-full bg-custom-light text-white font-extrabold py-3.5 rounded-xl hover:bg-custom-lightHover transition-all shadow-glow text-sm">Sign In</button>
                </div>
                <div class="flex items-center justify-between mt-4">
                    <p class="text-sm font-semibold text-gray-600">Don't have an account? <button onclick="showRegister()" class="text-custom-light font-extrabold">Sign Up</button></p>
                </div>
                <p class="text-center mt-4 text-xs text-gray-400"><a href="javascript:void(0)" onclick="showForgotPassword()" class="text-custom-light font-bold">Forgot Password?</a></p>
                <div class="mt-6 pt-6 border-t border-gray-100">
                    <button onclick="skipLogin()" class="w-full bg-white border-2 border-gray-200 text-gray-500 font-extrabold py-3.5 rounded-xl hover:bg-gray-50 text-sm">Skip for now & Explore</button>
                </div>
                <div class="mt-4 pt-4 border-t border-gray-50 text-center space-y-2">
                    <p class="text-[10px] text-gray-400 font-bold uppercase tracking-wider">Partner Access</p>
                    <button onclick="showPartnerLogin()" class="text-xs font-bold text-custom-dark hover:underline">Partner / Organizer Login</button>
                    <button onclick="showAdminLogin()" class="text-xs font-bold text-custom-dark hover:underline">Admin Login</button>
                </div>
            </div>

            <!-- Register Form -->
            <div id="registerFormCard" class="bg-white rounded-3xl shadow-2xl p-8 border border-gray-100 hidden">
                <div class="text-center mb-6">
                    <h2 class="text-xl font-extrabold text-gray-900">Create Account</h2>
                    <p class="text-sm text-gray-500 font-medium mt-1">Join EntryMySlot today</p>
                </div>
                <div id="regAlert" class="hidden mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200"></div>
                <div class="space-y-4">
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Full Name</label>
                        <div class="relative"><i class="fa-solid fa-user absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                        <input type="text" id="regName" placeholder="Your full name" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light focus:ring-1 focus:ring-custom-light text-sm font-bold bg-gray-50 transition-all" autocomplete="name"></div>
                    </div>
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Email</label>
                        <div class="relative"><i class="fa-solid fa-envelope absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                        <input type="email" id="regEmail" placeholder="you@example.com" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light focus:ring-1 focus:ring-custom-light text-sm font-bold bg-gray-50 transition-all" autocomplete="email"></div>
                    </div>
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Password</label>
                        <div class="relative"><i class="fa-solid fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                        <input type="password" id="regPassword" placeholder="Min 8 characters" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light focus:ring-1 focus:ring-custom-light text-sm font-bold bg-gray-50 transition-all" autocomplete="new-password"></div>
                    </div>
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Confirm Password</label>
                        <div class="relative"><i class="fa-solid fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                        <input type="password" id="regConfirmPassword" placeholder="Re-enter password" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light focus:ring-1 focus:ring-custom-light text-sm font-bold bg-gray-50 transition-all" autocomplete="new-password"></div>
                    </div>
                    <button onclick="handleCustomerRegister()" id="registerBtn" class="w-full bg-custom-dark text-white font-extrabold py-3.5 rounded-xl hover:bg-black transition-all shadow-md text-sm">Create Account</button>
                </div>
                <p class="text-center mt-6 text-sm font-semibold text-gray-600">Already have an account? <button onclick="showLogin()" class="text-custom-light font-extrabold">Sign In</button></p>
            </div>

            <!-- OTP Form -->
            <div id="otpFormCard" class="bg-white rounded-3xl shadow-2xl p-8 border border-gray-100 hidden">
                <div class="text-center mb-6">
                    <h2 class="text-xl font-extrabold text-gray-900">Verify Your Email</h2>
                    <p class="text-sm text-gray-500 font-medium mt-1">We've sent a 6-digit code to <br><span id="otpEmailDisplay" class="font-bold text-gray-800">your email</span></p>
                </div>
                <div id="otpAlert" class="hidden mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200"></div>
                <div class="flex justify-center gap-2 sm:gap-3" id="otpInputs">
                    <input type="text" maxlength="1" class="w-10 h-12 sm:w-12 sm:h-14 text-center text-xl font-extrabold border border-gray-200 rounded-xl bg-gray-50 text-gray-900" autocomplete="off">
                    <input type="text" maxlength="1" class="w-10 h-12 sm:w-12 sm:h-14 text-center text-xl font-extrabold border border-gray-200 rounded-xl bg-gray-50 text-gray-900" autocomplete="off">
                    <input type="text" maxlength="1" class="w-10 h-12 sm:w-12 sm:h-14 text-center text-xl font-extrabold border border-gray-200 rounded-xl bg-gray-50 text-gray-900" autocomplete="off">
                    <input type="text" maxlength="1" class="w-10 h-12 sm:w-12 sm:h-14 text-center text-xl font-extrabold border border-gray-200 rounded-xl bg-gray-50 text-gray-900" autocomplete="off">
                    <input type="text" maxlength="1" class="w-10 h-12 sm:w-12 sm:h-14 text-center text-xl font-extrabold border border-gray-200 rounded-xl bg-gray-50 text-gray-900" autocomplete="off">
                    <input type="text" maxlength="1" class="w-10 h-12 sm:w-12 sm:h-14 text-center text-xl font-extrabold border border-gray-200 rounded-xl bg-gray-50 text-gray-900" autocomplete="off">
                </div>
                <button onclick="handleVerifyOTP()" id="verifyOtpBtn" class="w-full bg-custom-dark text-white font-extrabold py-3.5 rounded-xl hover:bg-black transition-all shadow-md mt-6 text-sm">Verify Account</button>
                <p class="text-center mt-4 text-sm font-semibold text-gray-600">Didn't receive the code? <button onclick="handleResendOtp()" class="text-custom-light font-extrabold">Resend OTP</button></p>
                <p class="text-center mt-3"><button onclick="showRegister()" class="text-xs text-gray-400 hover:text-gray-700 flex items-center justify-center w-full gap-1"><i class="fa-solid fa-arrow-left"></i> Back to Sign Up</button></p>
            </div>

            <!-- Forgot Password -->
            <div id="forgotPasswordFormCard" class="bg-white rounded-3xl shadow-2xl p-8 border border-gray-100 hidden">
                <div class="text-center mb-6">
                    <h2 class="text-xl font-extrabold text-gray-900">Reset Password</h2>
                    <p class="text-sm text-gray-500 font-medium mt-1">Enter your email for a reset link</p>
                </div>
                <div id="forgotAlert" class="hidden mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200"></div>
                <div class="space-y-4">
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Email</label>
                        <div class="relative"><i class="fa-solid fa-envelope absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                        <input type="email" id="forgotEmail" placeholder="you@example.com" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light focus:ring-1 focus:ring-custom-light text-sm font-bold bg-gray-50 transition-all"></div>
                    </div>
                    <button onclick="handleForgotPassword()" class="w-full bg-custom-dark text-white font-extrabold py-3.5 rounded-xl hover:bg-black transition-all shadow-md text-sm">Send Reset Link</button>
                </div>
                <p class="text-center mt-6 text-sm"><button onclick="showLogin()" class="text-custom-light font-extrabold"><i class="fa-solid fa-arrow-left"></i> Back to Sign In</button></p>
            </div>

            <!-- Partner Login -->
            <div id="partnerLoginCard" class="bg-white rounded-3xl shadow-2xl p-8 border border-gray-100 hidden">
                <div class="text-center mb-6">
                    <h2 class="text-xl font-extrabold text-gray-900">Partner Portal</h2>
                    <p class="text-sm text-gray-500 font-medium mt-1">Organizer & Manager Login</p>
                </div>
                <div id="partnerAlert" class="hidden mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200"></div>
                <div class="space-y-4">
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Email</label>
                        <div class="relative"><i class="fa-solid fa-envelope absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                        <input type="email" id="partnerEmail" placeholder="partner@example.com" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light text-sm font-bold bg-gray-50 transition-all"></div>
                    </div>
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Password</label>
                        <div class="relative"><i class="fa-solid fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                        <input type="password" id="partnerPassword" placeholder="Your password" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light text-sm font-bold bg-gray-50 transition-all"></div>
                    </div>
                    <button onclick="handlePartnerLogin()" id="partnerLoginBtn" class="w-full bg-custom-dark text-white font-extrabold py-3.5 rounded-xl hover:bg-black transition-all shadow-md text-sm">Sign In</button>
                </div>
                <p class="text-center mt-6"><button onclick="showCustomerLogin()" class="text-custom-light font-extrabold text-sm"><i class="fa-solid fa-arrow-left"></i> Back to Customer Login</button></p>
            </div>

            <!-- Admin Login -->
            <div id="adminLoginCard" class="bg-white rounded-3xl shadow-2xl p-8 border border-gray-100 hidden">
                <div class="text-center mb-6">
                    <h2 class="text-xl font-extrabold text-gray-900">Admin Console</h2>
                    <p class="text-sm text-gray-500 font-medium mt-1">Super Admin Access</p>
                </div>
                <div id="adminAlert" class="hidden mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200"></div>
                <div class="space-y-4">
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Admin Email</label>
                        <div class="relative"><i class="fa-solid fa-envelope absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                        <input type="email" id="adminEmail" placeholder="admin@entrymyslot.com" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light text-sm font-bold bg-gray-50 transition-all"></div>
                    </div>
                    <div>
                        <label class="block text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-1.5">Password</label>
                        <div class="relative"><i class="fa-solid fa-lock absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 text-sm"></i>
                        <input type="password" id="adminPassword" placeholder="Admin password" class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light text-sm font-bold bg-gray-50 transition-all"></div>
                    </div>
                    <button onclick="handleAdminLogin()" id="adminLoginBtn" class="w-full bg-custom-dark text-white font-extrabold py-3.5 rounded-xl hover:bg-black transition-all shadow-md text-sm">Sign In</button>
                </div>
                <p class="text-center mt-6"><button onclick="showCustomerLogin()" class="text-custom-light font-extrabold text-sm"><i class="fa-solid fa-arrow-left"></i> Back</button></p>
            </div>
        </div>
    </div>
</div>

<!-- ==================== APP SHELL ==================== -->
<div id="appShell" class="view-section">

    <!-- Header -->
    <header class="bg-custom-dark text-white sticky top-0 z-50 shadow-md border-b border-custom-light/50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-14 sm:h-16">
                <div class="flex items-center flex-1">
                    <a href="/" class="flex-shrink-0 mr-4 sm:mr-6 flex items-center">
                        <img src="assets/images/logo.png" alt="EntryMySlot" class="h-8 sm:h-10 w-auto object-contain" onerror="this.style.display='none';this.nextElementSibling.style.display='block'">
                        <span class="hidden text-white font-extrabold text-lg tracking-tight" style="display:none">EMS</span>
                    </a>
                    <div class="hidden sm:block flex-1 max-w-2xl">
                        <div class="relative bg-white/10 rounded-xl flex items-center px-4 py-2 h-10 border border-white/10">
                            <i class="fa-solid fa-magnifying-glass text-white/70 mr-3 text-sm"></i>
                            <input type="text" id="heroSearchInput" class="bg-transparent border-none outline-none text-sm text-white w-full placeholder-white/70 font-medium" placeholder="Search Turfs, Events, Movies...">
                        </div>
                    </div>
                </div>
                <div class="flex items-center space-x-2 sm:space-x-4">
                    <button id="locationBtn" class="flex items-center text-white hover:text-custom-light text-xs sm:text-sm font-semibold transition bg-white/10 hover:bg-white/20 px-3 py-1.5 rounded-lg border border-white/15">
                        <i class="fa-solid fa-location-dot mr-1.5 text-custom-light"></i>
                        <span id="currentLocationDisplay" data-location-target="name" class="hidden sm:inline font-bold">Coimbatore</span>
                        <span id="currentLocationDisplayMobile" data-location-target="code" class="sm:hidden font-bold">CBE</span>
                    </button>
                    <div class="relative" id="profileDropdownContainer" style="display:none">
                        <button id="profileDropdownBtn" class="flex items-center text-white hover:text-custom-light text-xs sm:text-sm font-semibold transition">
                            <div class="w-8 h-8 rounded-full bg-custom-light flex items-center justify-center mr-1 sm:mr-2">
                                <span id="profileInitial" class="text-white font-extrabold text-sm">U</span>
                            </div>
                            <span id="profileNameHeader" class="hidden md:inline">User</span>
                            <i class="fa-solid fa-chevron-down ml-1 text-[10px]"></i>
                        </button>
                        <div id="profileDropdown" class="hidden absolute right-0 mt-2 w-56 bg-white rounded-2xl shadow-2xl border border-gray-100 z-50 overflow-hidden">
                            <div class="p-4 bg-gray-50 border-b border-gray-100">
                                <p id="dropdownName" class="font-extrabold text-sm text-gray-900">User Name</p>
                                <p id="dropdownEmail" class="text-xs text-gray-500 font-medium mt-0.5">user@email.com</p>
                            </div>
                            <div class="py-2" id="dropdownMenuLinks"></div>
                        </div>
                    </div>
                    <button class="text-white hover:text-custom-light transition md:hidden" onclick="toggleMobileNav()">
                        <i class="fa-solid fa-bars text-xl"></i>
                    </button>
                </div>
            </div>
        </div>
        <!-- Secondary Nav -->
        <nav class="hidden sm:block bg-white text-custom-dark text-sm border-b border-gray-100">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex items-center justify-between h-14">
                    <div class="flex space-x-8 whitespace-nowrap">
                        <a href="/explore/home" data-nav="home" class="nav-link font-bold text-custom-light border-b-2 border-custom-light transition py-4 flex items-center gap-2"><i class="fa-solid fa-futbol"></i> Sports</a>
                        <a href="/explore/movies" data-nav="movies" class="nav-link font-bold text-gray-500 hover:text-custom-dark transition py-4 border-b-2 border-transparent hover:border-custom-dark flex items-center gap-2"><i class="fa-solid fa-film"></i> Movies</a>
                        <a href="/explore/events" data-nav="events" class="nav-link font-bold text-gray-500 hover:text-custom-dark transition py-4 border-b-2 border-transparent hover:border-custom-dark flex items-center gap-2"><i class="fa-solid fa-ticket"></i> Events</a>
                    </div>
                    <div class="flex space-x-6 text-xs font-bold text-gray-500">
                        <a href="/partner/login" class="hover:text-custom-dark transition">Partner With Us</a>
                    </div>
                </div>
            </div>
        </nav>
    </header>

    <!-- Location Selector Overlay -->
    <div id="locationSelectorOverlay" class="fixed inset-0 z-[200] hidden bg-black/80 backdrop-blur-md flex items-center justify-center p-4">
        <div class="bg-white rounded-[24px] shadow-2xl w-full max-w-md overflow-hidden flex flex-col" id="locationSelectorContent">
            <div class="px-6 py-8 border-b border-gray-50 text-center bg-custom-dark text-white relative">
                <div class="w-16 h-16 mx-auto bg-white/10 rounded-full flex items-center justify-center mb-4 border border-white/20">
                    <i class="fa-solid fa-map-location-dot text-3xl text-custom-light"></i>
                </div>
                <h3 class="font-extrabold text-2xl tracking-tight">Where are you?</h3>
                <p class="text-sm text-gray-300 font-medium mt-2">Select your city to explore</p>
                <button onclick="EMSLocation.hideOverlay()" class="absolute top-4 right-4 text-white/60 hover:text-white transition bg-white/10 hover:bg-white/20 rounded-full w-8 h-8 flex items-center justify-center"><i class="fa-solid fa-xmark"></i></button>
            </div>
            <div class="p-6 overflow-y-auto max-h-[60vh] bg-[#FCFCFD]" id="locationSelectorBody">
                <button onclick="EMSLocation.attemptGeolocation(function(name){ if(name){ EMSLocation.setSelected(name); EMSLocation.hideOverlay(); EMSRouter.navigate('/explore/home'); } else { EMSLocation.setSelected('Coimbatore'); EMSLocation.hideOverlay(); EMSRouter.navigate('/explore/home'); } })" class="w-full mb-5 p-4 border-2 border-custom-light bg-custom-light/5 rounded-xl font-extrabold text-custom-dark text-sm transition-all flex items-center justify-center gap-2 hover:bg-custom-light hover:text-white">
                    <i class="fa-solid fa-location-crosshairs"></i> Detect My Current Location
                </button>
                <div class="relative mb-6">
                    <i class="fa-solid fa-magnifying-glass absolute left-4 top-1/2 -translate-y-1/2 text-gray-400"></i>
                    <input type="text" id="locationSearchInput" placeholder="Type your city..." class="w-full pl-11 pr-4 py-3.5 border border-gray-200 rounded-xl focus:outline-none focus:border-custom-light text-sm font-bold bg-white transition-all shadow-sm">
                </div>
                <h4 class="text-[10px] font-extrabold text-gray-400 uppercase tracking-widest mb-4 text-center">Popular Cities in Tamil Nadu</h4>
                <div class="grid grid-cols-3 gap-3" id="locationSelectorGrid"></div>
            </div>
        </div>
    </div>

    <!-- ==================== MAIN CONTENT AREA ==================== -->
    <main id="mainContent" class="min-h-screen">
        <!-- Dynamic content rendered here -->
    </main>

    <!-- Footer -->
    <footer class="bg-[#0f0f13] text-gray-400 pt-16 pb-12 border-t border-gray-800">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex flex-col md:flex-row items-center justify-between bg-white/5 px-8 py-10 rounded-[32px] mb-12 border border-white/10 shadow-2xl text-center md:text-left space-y-6 md:space-y-0 relative overflow-hidden">
                <div class="absolute top-0 right-0 w-96 h-full bg-gradient-to-l from-custom-light/10 to-transparent blur-3xl"></div>
                <div class="flex flex-col md:flex-row items-center relative z-10">
                    <div class="w-16 h-16 rounded-full bg-custom-light/20 border border-custom-light/30 flex items-center justify-center text-custom-light text-3xl md:mr-6 mb-4 md:mb-0"><i class="fa-solid fa-building-user"></i></div>
                    <div><h3 class="text-white font-extrabold text-xl sm:text-2xl mb-1">List your Venue</h3><p class="text-sm text-gray-400 font-medium">Partner with us to increase bookings.</p></div>
                </div>
                <a href="/partner/register" class="bg-white text-gray-900 px-8 py-3.5 rounded-xl shadow-md font-bold hover:bg-custom-light hover:text-white transition-all w-full md:w-auto relative z-10">Partner With Us</a>
            </div>
            <div class="border-t border-white/5 pt-10 text-center">
                <img src="assets/images/logo.png" alt="EntryMySlot" class="h-10 sm:h-12 w-auto object-contain mx-auto mb-8 opacity-80">
                <p class="text-[10px] sm:text-xs text-gray-500 font-semibold">Copyright 2026 &copy; EntryMySlot Entertainment Pvt. Ltd. All Rights Reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Mobile Bottom Nav -->
    <div class="md:hidden fixed bottom-0 left-0 w-full bg-white/90 backdrop-blur-md border-t border-gray-100 flex justify-around items-center px-2 py-2 shadow-[0_-10px_30px_rgba(0,0,0,0.08)] z-40" id="mobileBottomNav">
        <a href="/explore/home" class="mobile-nav-btn flex flex-col items-center text-custom-light p-2 rounded-xl w-16"><i class="fa-solid fa-house text-[22px] mb-1"></i><span class="text-[9px] font-extrabold uppercase">Home</span></a>
        <a href="/account/bookings" class="mobile-nav-btn flex flex-col items-center text-gray-400 hover:text-custom-light transition p-2 rounded-xl w-16"><i class="fa-regular fa-calendar-check text-[22px] mb-1"></i><span class="text-[9px] font-extrabold uppercase">Bookings</span></a>
        <a href="/account/profile" class="mobile-nav-btn flex flex-col items-center text-gray-400 hover:text-custom-light transition p-2 rounded-xl w-16"><i class="fa-regular fa-user text-[22px] mb-1"></i><span class="text-[9px] font-extrabold uppercase">Profile</span></a>
    </div>

</div>

<!-- ==================== GLOBAL ERROR TOAST ==================== -->
<div id="globalToast" class="fixed top-4 right-4 z-[9999] hidden max-w-sm bg-white rounded-2xl shadow-2xl border border-gray-100 p-4 transform translate-x-full transition-transform duration-300">
    <div class="flex items-start gap-3">
        <div id="toastIcon" class="w-8 h-8 rounded-full bg-red-50 flex items-center justify-center text-red-500 flex-shrink-0"><i class="fa-solid fa-circle-exclamation text-sm"></i></div>
        <div><p id="toastMessage" class="text-sm font-bold text-gray-900">Error</p></div>
    </div>
</div>

<!-- ==================== PAGE MODULES ==================== -->
<script>
// ── Location city grid ──────────────────────────────────────────
var POPULAR_CITIES = [
    { name: 'Chennai', code: 'CHN' }, { name: 'Coimbatore', code: 'CBE' }, { name: 'Madurai', code: 'MDU' },
    { name: 'Tiruchirappalli', code: 'TRC' }, { name: 'Salem', code: 'SLM' }, { name: 'Erode', code: 'ERD' },
];

<<<<<<< HEAD
    // ===== STATE =====
    let currentUser = null;
    let currentView = 'login';
    let selectedDistrict = 'Coimbatore';
    let selectedDistrictCode = 'CBE';
    
    let currentBookingState = { ground: null, groundId: '', selectedDate: '', selectedSlots: [], slots: [], turfName: '', turfImage: '' };
    let currentPaymentTotal = 0;
=======
function renderLocationGrid() {
    var grid = document.getElementById('locationSelectorGrid');
    if (!grid) return;
    grid.innerHTML = POPULAR_CITIES.map(function(city) {
        return '<button onclick="EMSLocation.setSelected(\'' + city.name + '\'); EMSLocation.hideOverlay(); EMSRouter.navigate('/explore/home')" class="flex flex-col items-center justify-center p-3 rounded-xl border border-gray-100 bg-white hover:bg-custom-light hover:text-white hover:border-custom-light transition-all shadow-sm hover:-translate-y-1">' +
            '<i class="fa-solid fa-city text-xl mb-2 text-custom-light group-hover:text-white"></i>' +
            '<span class="text-xs font-bold">' + city.name + '</span></button>';
    }).join('');
>>>>>>> d3846f2 (chore: finalize production frontend)

    // Search
    var search = document.getElementById('locationSearchInput');
    if (search) {
        search.addEventListener('input', function(e) {
            var q = e.target.value.toLowerCase();
            var items = grid.querySelectorAll('button');
            items.forEach(function(btn) {
                var text = btn.textContent.toLowerCase();
                btn.style.display = text.includes(q) ? '' : 'none';
            });
        });
    }
}

// ── Auth UI helpers ─────────────────────────────────────────────
function showAlert(elId, msg, type) {
    var el = document.getElementById(elId);
    if (!el) return;
    el.textContent = msg;
    el.className = 'mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border ' +
        (type === 'success' ? 'bg-green-50 text-green-600 border-green-200' : 'bg-red-50 text-red-600 border-red-200');
    el.classList.remove('hidden');
}

function setButtonLoading(btnId, text) {
    var btn = document.getElementById(btnId);
    if (!btn) return;
    btn.disabled = true;
    btn.innerHTML = '<i class="fa-solid fa-circle-notch fa-spin"></i> ' + text;
    btn.dataset.originalText = text;
}

function resetButton(btnId) {
    var btn = document.getElementById(btnId);
    if (!btn) return;
    btn.disabled = false;
    btn.innerHTML = btn.dataset.originalText || btn.textContent;
}

// ── Auth form handlers ──────────────────────────────────────────
async function handleCustomerLogin() {
    var email = document.getElementById('loginEmail').value.trim();
    var password = document.getElementById('loginPassword').value;
    if (!email || !password) { showAlert('loginAlert', 'Please fill in all fields.'); return; }
    setButtonLoading('loginBtn', 'Signing in...');
    try {
        var result = await EMSAuth.login(email, password);
        if (result.ok && result.data && result.data.success) {
            updateProfileUI();
            showAppView();
            EMSRouter.navigate('/explore/home');
        } else {
            showAlert('loginAlert', (result.data && result.data.message) || 'Invalid credentials.', 'error');
        }
    } catch (e) {
        showAlert('loginAlert', 'Login failed. Please try again.', 'error');
    } finally { resetButton('loginBtn'); }
}

async function handleCustomerRegister() {
    var name = document.getElementById('regName').value.trim();
    var email = document.getElementById('regEmail').value.trim();
    var password = document.getElementById('regPassword').value;
    var confirm = document.getElementById('regConfirmPassword').value;
    if (!name || !email || !password) { showAlert('regAlert', 'Please fill all required fields.'); return; }
    if (password !== confirm) { showAlert('regAlert', 'Passwords do not match.'); return; }
    if (password.length < 8) { showAlert('regAlert', 'Password must be at least 8 characters.'); return; }
    setButtonLoading('registerBtn', 'Sending OTP...');
    try {
        var result = await EMSAuth.register({ email: email, username: name, password: password });
        if (result.ok && result.data && result.data.success) {
            document.getElementById('registerFormCard').classList.add('hidden');
            document.getElementById('otpFormCard').classList.remove('hidden');
            document.getElementById('otpEmailDisplay').textContent = email;
            var otpInputs = document.querySelectorAll('#otpInputs input');
            otpInputs.forEach(function(i) { i.value = ''; });
            setTimeout(function() { if(otpInputs[0]) otpInputs[0].focus(); }, 50);
        } else {
            showAlert('regAlert', (result.data && result.data.message) || 'Registration failed.', 'error');
        }
    } catch (e) {
        showAlert('regAlert', 'Registration failed. Please try again.', 'error');
    } finally { resetButton('registerBtn'); }
}

async function handleVerifyOTP() {
    var inputs = document.querySelectorAll('#otpInputs input');
    var otp = Array.from(inputs).map(function(i){ return i.value; }).join('');
    if (otp.length < 6) { showAlert('otpAlert', 'Please enter a valid 6-digit OTP.'); return; }
    var email = document.getElementById('regEmail').value.trim();
    setButtonLoading('verifyOtpBtn', 'Verifying...');
    try {
        var result = await EMSAuth.verifyRegistrationOtp(email, otp);
        if (result.ok && result.data && result.data.success) {
            updateProfileUI();
            showAppView();
            EMSRouter.navigate('/explore/home');
        } else {
            showAlert('otpAlert', (result.data && result.data.message) || 'Invalid OTP.', 'error');
        }
    } catch (e) {
        showAlert('otpAlert', 'Verification failed. Please try again.', 'error');
    } finally { resetButton('verifyOtpBtn'); }
}

async function handleResendOtp() {
    var email = document.getElementById('regEmail').value.trim();
    try {
        var result = await EMSAuth.resendRegistrationOtp(email);
        showAlert('otpAlert', (result.data && result.data.message) || 'OTP resent!', 'success');
    } catch (e) {
        showAlert('otpAlert', 'Failed to resend OTP.', 'error');
    }
}

function showForgotPassword() {
    document.getElementById('loginFormCard').classList.add('hidden');
    document.getElementById('forgotPasswordFormCard').classList.remove('hidden');
    document.getElementById('forgotAlert').classList.add('hidden');
}

async function handleForgotPassword() {
    var email = document.getElementById('forgotEmail').value.trim();
    if (!email) { showAlert('forgotAlert', 'Please enter your email.'); return; }
    setButtonLoading(document.querySelector('#forgotPasswordFormCard button'), 'Sending...');
    try {
        var result = await EMSAuth.forgotPassword(email);
        showAlert('forgotAlert', (result.data && result.data.message) || 'If an account exists, a reset link has been sent.', 'success');
    } catch (e) {
        showAlert('forgotAlert', 'Something went wrong.', 'error');
    } finally { resetButton(document.querySelector('#forgotPasswordFormCard button')); }
}

function showLogin() {
    document.getElementById('loginFormCard').classList.remove('hidden');
    document.getElementById('registerFormCard').classList.add('hidden');
    document.getElementById('otpFormCard').classList.add('hidden');
    document.getElementById('forgotPasswordFormCard').classList.add('hidden');
    document.getElementById('partnerLoginCard').classList.add('hidden');
    document.getElementById('adminLoginCard').classList.add('hidden');
    document.getElementById('authSubtitle').textContent = 'Book Sports Venues & Turfs Near You';
}

function showRegister() {
    document.getElementById('loginFormCard').classList.add('hidden');
    document.getElementById('registerFormCard').classList.remove('hidden');
    document.getElementById('otpFormCard').classList.add('hidden');
    document.getElementById('forgotPasswordFormCard').classList.add('hidden');
    document.getElementById('partnerLoginCard').classList.add('hidden');
    document.getElementById('adminLoginCard').classList.add('hidden');
}

function showPartnerLogin() {
    document.getElementById('loginFormCard').classList.add('hidden');
    document.getElementById('registerFormCard').classList.add('hidden');
    document.getElementById('partnerLoginCard').classList.remove('hidden');
    document.getElementById('adminLoginCard').classList.add('hidden');
    document.getElementById('authSubtitle').textContent = 'Organizer & Manager Portal';
}

function showAdminLogin() {
    document.getElementById('loginFormCard').classList.add('hidden');
    document.getElementById('registerFormCard').classList.add('hidden');
    document.getElementById('partnerLoginCard').classList.add('hidden');
    document.getElementById('adminLoginCard').classList.remove('hidden');
    document.getElementById('authSubtitle').textContent = 'Admin Console';
}

function showCustomerLogin() {
    document.getElementById('loginFormCard').classList.remove('hidden');
    document.getElementById('registerFormCard').classList.add('hidden');
    document.getElementById('partnerLoginCard').classList.add('hidden');
    document.getElementById('adminLoginCard').classList.add('hidden');
    document.getElementById('authSubtitle').textContent = 'Book Sports Venues & Turfs Near You';
}

async function handlePartnerLogin() {
    var email = document.getElementById('partnerEmail').value.trim();
    var password = document.getElementById('partnerPassword').value;
    if (!email || !password) { showAlert('partnerAlert', 'Please fill in all fields.'); return; }
    setButtonLoading('partnerLoginBtn', 'Signing in...');
    try {
        var result = await EMSOrganizerAuth.login(email, password);
        if (result.ok && result.data && result.data.success) {
            hideAuthOverlay();
            EMSRouter.navigate('/partner/dashboard');
        } else {
            showAlert('partnerAlert', (result.data && result.data.message) || 'Invalid credentials.', 'error');
        }
    } catch (e) {
        showAlert('partnerAlert', 'Login failed.', 'error');
    } finally { resetButton('partnerLoginBtn'); }
}

async function handleAdminLogin() {
    var email = document.getElementById('adminEmail').value.trim();
    var password = document.getElementById('adminPassword').value;
    if (!email || !password) { showAlert('adminAlert', 'Please fill in all fields.'); return; }
    setButtonLoading('adminLoginBtn', 'Signing in...');
    try {
        var result = await EMSAdminAuth.login(email, password);
        if (result.ok && result.data && result.data.success) {
            hideAuthOverlay();
            EMSRouter.navigate('/admin/dashboard');
        } else {
            showAlert('adminAlert', (result.data && result.data.message) || 'Invalid credentials.', 'error');
        }
    } catch (e) {
        showAlert('adminAlert', 'Login failed.', 'error');
    } finally { resetButton('adminLoginBtn'); }
}

function skipLogin() {
    hideAuthOverlay();
    showAppView();
    EMSRouter.navigate('/explore/home');
}

async function handleCustomerLogout() {
    try { await EMSAuth.logout(); } catch (_) {}
    currentUser = null;
    hideAppView();
    showAuthOverlay();
    showLogin();
}

// ── View management ────────────────────────────────────────────
function showAuthOverlay() {
    document.getElementById('authOverlay').classList.add('active');
    document.getElementById('appShell').classList.remove('active');
    document.getElementById('mainContent').innerHTML = '';
}

function hideAuthOverlay() {
    document.getElementById('authOverlay').classList.remove('active');
}

function showAppView() {
    hideAuthOverlay();
    document.getElementById('appShell').classList.add('active');
    document.getElementById('profileDropdownContainer').style.display = '';
    updateProfileUI();
}

function hideAppView() {
    document.getElementById('appShell').classList.remove('active');
    document.getElementById('profileDropdownContainer').style.display = 'none';
}

function updateProfileUI() {
    var user = EMSAuth.getUser();
    var profileContainer = document.getElementById('profileDropdownContainer');
    if (!user) {
        if (profileContainer) profileContainer.style.display = 'none';
        return;
    }
    if (profileContainer) profileContainer.style.display = '';
    var initial = (user.username || user.name || 'U').charAt(0).toUpperCase();
    var initialEl = document.getElementById('profileInitial');
    var nameEl = document.getElementById('profileNameHeader');
    var dropdownName = document.getElementById('dropdownName');
    var dropdownEmail = document.getElementById('dropdownEmail');
    var links = document.getElementById('dropdownMenuLinks');
    if (initialEl) initialEl.textContent = initial;
    if (nameEl) nameEl.textContent = user.username || user.name || 'User';
    if (dropdownName) dropdownName.textContent = user.username || user.name || 'User';
    if (dropdownEmail) dropdownEmail.textContent = user.email || '';
    if (links) {
        links.innerHTML = '<a href="/account/profile" class="w-full text-left px-4 py-2.5 text-sm font-bold text-gray-700 hover:bg-gray-50 transition flex items-center gap-3"><i class="fa-solid fa-user text-gray-400 w-5"></i> My Profile</a>' +
            '<a href="/account/bookings" class="w-full text-left px-4 py-2.5 text-sm font-bold text-gray-700 hover:bg-gray-50 transition flex items-center gap-3"><i class="fa-solid fa-calendar-check text-gray-400 w-5"></i> My Bookings</a>' +
            '<div class="border-t border-gray-100 my-1"></div>' +
            '<button onclick="handleCustomerLogout()" class="w-full text-left px-4 py-2.5 text-sm font-bold text-red-600 hover:bg-red-50 transition flex items-center gap-3"><i class="fa-solid fa-right-from-bracket w-5"></i> Logout</button>';
    }
}

// ── Profile dropdown toggle ────────────────────────────────────
document.addEventListener('click', function(e) {
    var dropdown = document.getElementById('profileDropdown');
    var btn = document.getElementById('profileDropdownBtn');
    if (dropdown && btn && !btn.contains(e.target) && !dropdown.contains(e.target)) {
        dropdown.classList.add('hidden');
    }
});
if (document.getElementById('profileDropdownBtn')) {
    document.getElementById('profileDropdownBtn').addEventListener('click', function(e) {
        e.stopPropagation();
        document.getElementById('profileDropdown').classList.toggle('hidden');
    });
}

// ── Location button ────────────────────────────────────────────
document.getElementById('locationBtn')?.addEventListener('click', function() {
    EMSLocation.showOverlay({ onSelect: function() { EMSRouter.navigate('/explore/home'); } });
});

// ── Mobile nav ─────────────────────────────────────────────────
function toggleMobileNav() {
    var sidebar = document.getElementById('mobileSidebar');
    if (sidebar) sidebar.classList.toggle('translate-x-full');
}

// ── Toast notification ─────────────────────────────────────────
function showToast(message, type) {
    type = type || 'error';
    var toast = document.getElementById('globalToast');
    var msgEl = document.getElementById('toastMessage');
    var iconEl = document.getElementById('toastIcon');
    if (!toast) return;
    msgEl.textContent = message;
    if (type === 'error') {
        iconEl.className = 'w-8 h-8 rounded-full bg-red-50 flex items-center justify-center text-red-500 flex-shrink-0';
        iconEl.innerHTML = '<i class="fa-solid fa-circle-exclamation text-sm"></i>';
    } else {
        iconEl.className = 'w-8 h-8 rounded-full bg-green-50 flex items-center justify-center text-green-500 flex-shrink-0';
        iconEl.innerHTML = '<i class="fa-solid fa-circle-check text-sm"></i>';
    }
<<<<<<< HEAD

    function showRegAlert(msg, type) {
        var alertBox = document.getElementById('regAlert');
        if (!alertBox) {
            console.error(msg);
            return;
        }
        if (!type) type = 'error';
        var cls = type === 'success'
            ? 'mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-green-50 text-green-600 border-green-200'
            : 'mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200';
        alertBox.textContent = msg;
        alertBox.className = cls;
        alertBox.classList.remove('hidden');
    }

    function showOtpAlert(msg, type) {
        var alertBox = document.getElementById('otpAlert');
        if (!alertBox) {
            console.error(msg);
            return;
        }
        if (!type) type = 'error';
        var cls = type === 'success'
            ? 'mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-green-50 text-green-600 border-green-200'
            : 'mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200';
        alertBox.textContent = msg;
        alertBox.className = cls;
        alertBox.classList.remove('hidden');
    }

    // ===== LOCATION DISPLAY UPDATE =====
    function updateLocationDisplay() {
        const desk = document.getElementById('currentLocationDisplay');
        const mob = document.getElementById('currentLocationDisplayMobile');
        const home = document.getElementById('homeLocationName');
        const profileLoc = document.getElementById('profileDetailLocation');
        
        if (desk) desk.textContent = selectedDistrict;
        if (mob) mob.textContent = selectedDistrictCode;
        if (home) home.textContent = selectedDistrict;
        if (profileLoc) profileLoc.textContent = selectedDistrict;
    }

    // ===== SHARED LOCATION SELECTOR =====
    function setupLocationSelector() {
        const searchInput = document.getElementById('locationSelectorSearch');
        const suggestionsBox = document.getElementById('locationSelectorSuggestions');

        if (searchInput && suggestionsBox) {
            searchInput.addEventListener('input', (e) => {
                const query = e.target.value.toLowerCase().trim();
                suggestionsBox.innerHTML = '';
                if (query.length > 0) {
                    const matches = TN_DISTRICTS.filter(d => d.name.toLowerCase().includes(query));
                    if (matches.length > 0) {
                        suggestionsBox.classList.remove('hidden');
                        matches.forEach(match => {
                            const div = document.createElement('div');
                            div.className = 'px-4 py-3 cursor-pointer hover:bg-gray-50 font-bold text-sm text-gray-700 flex items-center gap-2 transition-colors';
                            div.innerHTML = `<i class="fa-solid fa-location-dot text-custom-light"></i> ${match.name}`;
                            div.onclick = () => {
                                searchInput.value = match.name;
                                suggestionsBox.classList.add('hidden');
                                selectLocationFromSelector(match.name, match.code);
                            };
                            suggestionsBox.appendChild(div);
                        });
                    } else {
                        suggestionsBox.classList.remove('hidden');
                        suggestionsBox.innerHTML = '<div class="px-4 py-3 text-sm text-gray-500 font-medium">No cities found</div>';
                    }
                } else { 
                    suggestionsBox.classList.add('hidden'); 
                }
            });
            document.addEventListener('click', (e) => {
                if (!searchInput.contains(e.target) && !suggestionsBox.contains(e.target)) {
                    suggestionsBox.classList.add('hidden');
                }
            });
        }
    }

    function openLocationSelector() {
        const overlay = document.getElementById('locationSelectorOverlay');
        const content = document.getElementById('locationSelectorContent');
        overlay.classList.remove('hidden');
        document.getElementById('locationSelectorSearch').value = '';
        document.getElementById('locationSelectorSuggestions').classList.add('hidden');
        document.body.style.overflow = 'hidden';

        setTimeout(() => { 
            overlay.classList.remove('opacity-0'); 
            content.classList.remove('scale-95'); 
        }, 10);
    }

    function closeLocationSelector() {
        const overlay = document.getElementById('locationSelectorOverlay');
        const content = document.getElementById('locationSelectorContent');
        if (!overlay || overlay.classList.contains('hidden')) return;
        overlay.classList.add('opacity-0');
        content.classList.add('scale-95');
        document.body.style.overflow = '';
        setTimeout(() => { overlay.classList.add('hidden'); }, 300);
    }

    function selectLocationFromSelector(name, code) {
        saveGlobalLocation(name, code);
        closeLocationSelector();
        
        // Show loading state
        const loadingEl = document.createElement('div');
        loadingEl.className = 'fixed inset-0 bg-white/80 backdrop-blur-sm z-[300] flex flex-col items-center justify-center';
        loadingEl.innerHTML = '<div class="w-10 h-10 border-4 border-gray-200 border-t-custom-light rounded-full animate-spin mb-4"></div><p class="font-extrabold text-gray-900">Updating venues for ' + name + '...</p>';
        document.body.appendChild(loadingEl);
        setTimeout(() => { loadingEl.remove(); loadTurfs(); }, 600);
    }

    // ===== AUTH =====
    async function restoreCustomerSession() {
        try {
            var user = await EMS_AUTH.restoreSession();
            if (user) {
                currentUser = user;
                showAppView();
            } else {
                // If they previously skipped login, respect that choice across page reloads
                if (localStorage.getItem('ems_skipped_login') === 'true') {
                    currentUser = null;
                    showAppView();
                } else {
                    showLoginView();
                }
            }
        } catch (e) {
            console.error('Session restore failed:', e);
            if (localStorage.getItem('ems_skipped_login') === 'true') {
                currentUser = null;
                showAppView();
            } else {
                showLoginView();
            }
        }
    }

    function skipLogin() {
        currentUser = null; // Guest user
        localStorage.setItem('ems_skipped_login', 'true'); // Persist user's choice to skip
        showAppView();
    }

    function showLogin() {
        document.getElementById('loginFormCard').classList.remove('hidden');
        document.getElementById('registerFormCard').classList.add('hidden');
        document.getElementById('forgotPasswordFormCard').classList.add('hidden');
        if (document.getElementById('otpFormCard')) {
            document.getElementById('otpFormCard').classList.add('hidden');
        }
    }

    function showRegister() {
        document.getElementById('loginFormCard').classList.add('hidden');
        document.getElementById('registerFormCard').classList.remove('hidden');
        document.getElementById('forgotPasswordFormCard').classList.add('hidden');
        if (document.getElementById('otpFormCard')) {
            document.getElementById('otpFormCard').classList.add('hidden');
        }
    }

    async function handleLogin() {
        var email = document.getElementById('loginEmail').value.trim();
        var password = document.getElementById('loginPassword').value;

        if (!email || !password) {
            showAlert('Please enter both email and password.');
            return;
        }

        var btn = document.querySelector('#loginFormCard button[onclick="handleLogin()"]');
        var originalText = btn ? btn.innerHTML : 'Sign In';
        if (btn) { btn.disabled = true; btn.innerHTML = '<i class="fa-solid fa-circle-notch fa-spin"></i> Signing in...'; }

        try {
            var result = await EMS_AUTH.login(email, password);

            if (result.ok && result.data && result.data.success) {
                currentUser = EMS_AUTH.getUser();
                showAppView();
            } else {
                var msg = (result.data && result.data.message) || 'Invalid email or password.';
                showAlert(msg);
            }
        } catch (err) {
            console.error('Login error:', err);
            showAlert('Login failed. Please try again.');
        } finally {
            if (btn) { btn.disabled = false; btn.innerHTML = originalText; }
        }
    }

    async function handleRegister() {
        var name = document.getElementById('regName').value.trim();
        var phone = document.getElementById('regPhone').value.trim();
        var email = document.getElementById('regEmail').value.trim();
        var password = document.getElementById('regPassword').value;
        var confirm = document.getElementById('regConfirmPassword').value;
        var alertBox = document.getElementById('regAlert');

        if (!name || !email || !password) { showRegAlert('Please fill in all required fields.', 'error'); return; }
        if (password !== confirm) { showRegAlert('Passwords do not match.', 'error'); return; }
        if (password.length < 8) { showRegAlert('Password must be at least 8 characters.', 'error'); return; }

        var btn = document.querySelector('#registerFormCard button[onclick="handleRegister()"]');
        var originalText = btn ? btn.innerHTML : 'Create Account';
        if (btn) { btn.disabled = true; btn.innerHTML = '<i class="fa-solid fa-circle-notch fa-spin"></i> Sending OTP...'; }
        if (alertBox) alertBox.classList.add('hidden');

        try {
            var result = await EMS_AUTH.register({
                email: email,
                username: name,
                password: password
            });

            if (result.ok && result.data && result.data.success) {
                document.getElementById('registerFormCard').classList.add('hidden');
                var otpCard = document.getElementById('otpFormCard');
                if (otpCard) {
                    otpCard.classList.remove('hidden');
                    document.getElementById('otpEmailDisplay').textContent = email;
                    document.querySelectorAll('#otpInputs input').forEach(input => input.value = '');
                    setTimeout(function() {
                        var firstOtp = document.querySelector('#otpInputs input');
                        if (firstOtp) firstOtp.focus();
                    }, 50);
                }
            } else {
                var msg = (result.data && result.data.message) || 'Registration failed. Please try again.';
                showRegAlert(msg, 'error');
            }
        } catch (err) {
            console.error('Registration error:', err);
            showRegAlert('Registration failed. Please try again.', 'error');
        } finally {
            if (btn) { btn.disabled = false; btn.innerHTML = originalText; }
        }
    }

    async function handleVerifyOTP() {
        var inputs = document.querySelectorAll('#otpInputs input');
        var otp = Array.from(inputs).map(i => i.value).join('');
        var alertBox = document.getElementById('otpAlert');

        if (otp.length < 6) {
            showOtpAlert('Please enter a valid 6-digit OTP.', 'error');
            return;
        }

        if (alertBox) alertBox.classList.add('hidden');
        var btn = document.getElementById('verifyOtpBtn');
        var originalText = btn ? btn.innerHTML : 'Verify Account';
        if (btn) {
            btn.disabled = true;
            btn.innerHTML = '<i class="fa-solid fa-circle-notch fa-spin"></i> Verifying...';
        }

        try {
            var email = document.getElementById('regEmail').value.trim();
            var result = await EMS_AUTH.verifyRegistrationOtp(email, otp);

            if (result.ok && result.data && result.data.success) {
                currentUser = EMS_AUTH.getUser();
                showAppView();
            } else {
                var msg = (result.data && result.data.message) || 'Invalid OTP. Please check your code.';
                showOtpAlert(msg, 'error');
            }
        } catch (err) {
            console.error('Verification error:', err);
            showOtpAlert('Verification failed. Please try again.', 'error');
        } finally {
            if (btn) { btn.disabled = false; btn.innerHTML = originalText; }
        }
    }

    async function handleResendOtp() {
        var email = document.getElementById('regEmail').value.trim();
        var btn = document.querySelector('#otpFormCard button');
        var originalText = btn ? btn.innerHTML : 'Resend OTP';

        if (!email) return;

        try {
            var result = await EMS_AUTH.resendRegistrationOtp(email);
            if (result.ok && result.data && result.data.success) {
                showOtpAlert('A new OTP has been sent to your email.', 'success');
            } else {
                var msg = (result.data && result.data.message) || 'Failed to resend OTP.';
                showOtpAlert(msg, 'error');
            }
        } catch (err) {
            console.error('Resend OTP error:', err);
            showOtpAlert('Failed to resend OTP. Please try again.', 'error');
        }
    }

    function openForgotPassword() {
        document.getElementById('loginFormCard').classList.add('hidden');
        document.getElementById('registerFormCard').classList.add('hidden');
        document.getElementById('forgotPasswordFormCard').classList.remove('hidden');
        var forgotAlert = document.getElementById('forgotAlert');
        if (forgotAlert) forgotAlert.classList.add('hidden');
        document.getElementById('forgotEmail').value = '';
    }

    async function handleForgotPassword() {
        var email = document.getElementById('forgotEmail').value.trim();
        var alertBox = document.getElementById('forgotAlert');
        var btn = document.querySelector('#forgotPasswordFormCard button');
        var originalText = btn ? btn.innerHTML : 'Send Reset Link';

        if (!email) {
            if (alertBox) {
                alertBox.textContent = 'Please enter your email address.';
                alertBox.className = 'mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200';
                alertBox.classList.remove('hidden');
            }
            return;
        }

        if (btn) { btn.disabled = true; btn.innerHTML = '<i class="fa-solid fa-circle-notch fa-spin"></i> Sending...'; }
        if (alertBox) alertBox.classList.add('hidden');

        try {
            var result = await EMS_AUTH.forgotPassword(email);
            if (result.ok && result.data && result.data.success) {
                if (alertBox) {
                    alertBox.textContent = result.data.message || 'If an account with that email exists, a password reset link has been sent.';
                    alertBox.className = 'mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-green-50 text-green-600 border-green-200';
                    alertBox.classList.remove('hidden');
                }
            } else {
                if (alertBox) {
                    alertBox.textContent = (result.data && result.data.message) || 'Something went wrong. Please try again.';
                    alertBox.className = 'mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200';
                    alertBox.classList.remove('hidden');
                }
            }
        } catch (err) {
            console.error('Forgot password error:', err);
            if (alertBox) {
                alertBox.textContent = 'Something went wrong. Please try again.';
                alertBox.className = 'mb-4 px-4 py-3 rounded-xl text-sm font-bold text-center border bg-red-50 text-red-600 border-red-200';
                alertBox.classList.remove('hidden');
            }
        } finally {
            if (btn) { btn.disabled = false; btn.innerHTML = originalText; }
        }
    }

    async function handleLogout() {
        try {
            await EMS_AUTH.logout();
        } catch (e) { /* best effort */ }
        if (currentUser) {
            localStorage.removeItem('ems_location_selected_' + currentUser.email);
        }
        localStorage.removeItem('ems_skipped_login'); // Reset skip flag on explicit logout
        currentUser = null;
        showLoginView();
    }

    // ===== VIEW MANAGEMENT =====
    function showLoginView() {
        document.getElementById('loginView').classList.add('active');
        document.getElementById('appView').classList.add('active'); // keep background visible
        document.getElementById('homeView').classList.add('active');
        
        ['bookingView', 'paymentView', 'profileView', 'mybookingsView'].forEach(id => {
            document.getElementById(id)?.classList.remove('active');
        });
        currentView = 'login';
    }

    function showAppView() {
        document.getElementById('loginView').classList.remove('active');
        document.getElementById('appView').classList.add('active');
        updateProfileUI();
        navigateTo('home');

        setTimeout(() => {
            const userLocKey = currentUser ? 'ems_location_selected_' + currentUser.email : 'ems_location_selected_guest';
            if (!localStorage.getItem(userLocKey)) {
                // Show the shared location selector on first entry
                openLocationSelector();
            }
        }, 300);
    }

    function navigateTo(view) {
        const protectedViews = ['booking', 'payment', 'profile', 'mybookings'];
        
        // Ensure user is logged in to proceed to protected views (like booking)
        if (!currentUser && protectedViews.includes(view)) {
            showAlert('Please log in or create an account to access this feature.', 'error');
            showLoginView();
            return;
        }
        
        ['homeView', 'bookingView', 'paymentView', 'profileView', 'mybookingsView'].forEach(id => {
            document.getElementById(id)?.classList.remove('active');
        });
        document.getElementById('appFooter').style.display = '';

        switch(view) {
            case 'home':
                document.getElementById('homeView').classList.add('active');
                window.history.pushState(null, '', window.location.pathname);
                break;
            case 'booking':
                document.getElementById('bookingView').classList.add('active');
                window.history.pushState(null, '', '#booking');
                break;
            case 'payment':
                document.getElementById('paymentView').classList.add('active');
                document.getElementById('appFooter').style.display = 'none';
                window.history.pushState(null, '', '#payment');
                break;
            case 'profile':
                document.getElementById('profileView').classList.add('active');
                document.getElementById('appFooter').style.display = 'none';
                updateProfilePage();
                window.history.pushState(null, '', '#profile');
                break;
            case 'mybookings':
                document.getElementById('mybookingsView').classList.add('active');
                renderMyBookings();
                window.history.pushState(null, '', '#mybookings');
                break;
        }
        currentView = view;
        window.scrollTo({ top: 0, behavior: 'smooth' });
        updateActiveNav(view);
    }

    function updateActiveNav(view) {
        document.querySelectorAll('.nav-link').forEach(link => {
            link.classList.remove('text-custom-light', 'border-custom-light');
            link.classList.add('text-gray-500', 'border-transparent');
            if (link.dataset.nav === view) {
                link.classList.remove('text-gray-500', 'border-transparent');
                link.classList.add('text-custom-light', 'border-custom-light');
            }
        });
    }

    // ===== PROFILE UI =====
    function updateProfileUI() {
        const dropdownLinks = document.getElementById('dropdownMenuLinks');
        const sidebarAccountLinks = document.getElementById('sidebarAccountLinks');
        
        // If it's a guest user who skipped login
        if (!currentUser) {
            document.getElementById('profileInitial').textContent = 'G';
            document.getElementById('profileNameHeader').textContent = 'Guest';
            
            const dropdownName = document.getElementById('dropdownName');
            const dropdownEmail = document.getElementById('dropdownEmail');
            if (dropdownName) dropdownName.textContent = 'Guest User';
            if (dropdownEmail) dropdownEmail.textContent = 'Sign in to book tickets';
            
            if(dropdownLinks) {
                dropdownLinks.innerHTML = `
                    <button onclick="showLoginView()" class="w-full text-left px-4 py-2.5 text-sm font-bold text-custom-light hover:bg-gray-50 transition flex items-center gap-3">
                        <i class="fa-solid fa-right-to-bracket w-5"></i> Sign In / Register
                    </button>
                `;
            }
            if(sidebarAccountLinks) {
                sidebarAccountLinks.innerHTML = `
                    <a href="#" onclick="showLoginView(); toggleSidebar();" class="block p-3 rounded-xl hover:bg-white hover:shadow-soft font-bold transition flex items-center text-custom-light"><i class="fa-solid fa-right-to-bracket w-6 text-center mr-3"></i> Sign In / Register</a>
                `;
            }
            return;
        }
        
        // If it's a fully logged-in user
        const initial = currentUser.name ? currentUser.name.charAt(0).toUpperCase() : 'U';
        document.getElementById('profileInitial').textContent = initial;
        document.getElementById('profileNameHeader').textContent = currentUser.name || 'User';
        
        const dropdownName = document.getElementById('dropdownName');
        const dropdownEmail = document.getElementById('dropdownEmail');
        if (dropdownName) dropdownName.textContent = currentUser.name || 'User';
        if (dropdownEmail) dropdownEmail.textContent = currentUser.email || '';
        
        if(dropdownLinks) {
            dropdownLinks.innerHTML = `
                <button onclick="navigateTo('profile')" class="w-full text-left px-4 py-2.5 text-sm font-bold text-gray-700 hover:bg-gray-50 transition flex items-center gap-3">
                    <i class="fa-solid fa-user text-gray-400 w-5"></i> My Profile
                </button>
                <button onclick="navigateTo('mybookings')" class="w-full text-left px-4 py-2.5 text-sm font-bold text-gray-700 hover:bg-gray-50 transition flex items-center gap-3">
                    <i class="fa-solid fa-calendar-check text-gray-400 w-5"></i> My Bookings
                </button>
                <div class="border-t border-gray-100 my-1"></div>
                <button onclick="handleLogout()" class="w-full text-left px-4 py-2.5 text-sm font-bold text-red-600 hover:bg-red-50 transition flex items-center gap-3">
                    <i class="fa-solid fa-right-from-bracket w-5"></i> Logout
                </button>
            `;
        }
        
        if(sidebarAccountLinks) {
            sidebarAccountLinks.innerHTML = `
                <a href="#" onclick="navigateTo('profile'); toggleSidebar();" class="block p-3 rounded-xl hover:bg-white hover:shadow-soft font-bold transition text-gray-700"><i class="fa-regular fa-user w-6 text-center mr-3 text-custom-light"></i> My Profile</a>
                <a href="#" onclick="handleLogout(); toggleSidebar();" class="block p-3 rounded-xl hover:bg-red-50 font-bold transition text-red-600"><i class="fa-solid fa-right-from-bracket w-6 text-center mr-3"></i> Logout</a>
            `;
        }
    }

    function updateProfilePage() {
        if (!currentUser) return;
        const initial = currentUser.name ? currentUser.name.charAt(0).toUpperCase() : 'U';
        document.getElementById('profileAvatarLetter').textContent = initial;
        document.getElementById('profileFullName').textContent = currentUser.name || 'User';
        document.getElementById('profileEmailDisplay').textContent = currentUser.email || '';
        document.getElementById('profileDetailName').textContent = currentUser.name || 'Not provided';
        document.getElementById('profileDetailEmail').textContent = currentUser.email || 'Not provided';
        document.getElementById('profileDetailPhone').textContent = currentUser.phone || 'Not provided';
        document.getElementById('profileDetailLocation').textContent = selectedDistrict || 'Not selected';
    }

    // ===== LOCATION STATE MANAGEMENT =====
    function saveGlobalLocation(name, code) {
        localStorage.setItem('ems_global_location_name', name);
        localStorage.setItem('ems_global_location_code', code);
        
        selectedDistrict = name;
        selectedDistrictCode = code || name.substring(0, 3).toUpperCase();
        
        updateLocationDisplay();
        
        // Save that user has selected location
        const userLocKey = currentUser ? 'ems_location_selected_' + currentUser.email : 'ems_location_selected_guest';
        localStorage.setItem(userLocKey, 'true');
    }

    // Helper to calculate nearest district from latitude and longitude coordinates
    function findClosestDistrict(lat, lon) {
        let minDistance = Infinity;
        let closest = null;
        for (const d of TN_DISTRICTS) {
            if (d.lat && d.lng) {
                const dLat = (d.lat - lat) * Math.PI / 180;
                const dLon = (d.lng - lon) * Math.PI / 180;
                const a = Math.sin(dLat / 2) * Math.sin(dLat / 2) +
                          Math.cos(lat * Math.PI / 180) * Math.cos(d.lat * Math.PI / 180) *
                          Math.sin(dLon / 2) * Math.sin(dLon / 2);
                const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
                const distance = 6371 * c; // distance in kilometers
                if (distance < minDistance) {
                    minDistance = distance;
                    closest = d;
                }
            }
        }
        return closest;
    }

    // ===== LIVE LOCATION RESOLUTION (shared) =====
    async function useLiveLocation() {
        const btn = document.getElementById('locationSelectorLiveBtn');
        if (!btn) return;

        const originalText = btn.innerHTML;
        btn.innerHTML = '<div class="w-5 h-5 border-2 border-custom-dark border-t-transparent rounded-full animate-spin"></div> Detecting Location...';
        btn.disabled = true;

        if (!navigator.geolocation) {
            const searchInput = document.getElementById('locationSelectorSearch');
            if (searchInput) {
                searchInput.value = 'Geolocation not supported.';
                setTimeout(() => searchInput.value = '', 2000);
            }
            btn.innerHTML = originalText;
            btn.disabled = false;
            return;
        }

        navigator.geolocation.getCurrentPosition(
            async (position) => {
                const lat = position.coords.latitude;
                const lon = position.coords.longitude;

                let detectedName = '';
                let detectedCode = '';

                // Attempt Reverse Geocoding via OpenStreetMap Nominatim
                try {
                    const response = await fetch(`https://nominatim.openstreetmap.org/reverse?lat=${lat}&lon=${lon}&format=json&accept-language=en`, {
                        headers: { 'Accept': 'application/json' }
                    });
                    if (response.ok) {
                        const data = await response.json();
                        const addr = data.address || {};
                        const rawCity = addr.city || addr.town || addr.state_district || addr.county || addr.suburb || addr.municipality || '';
                        
                        if (rawCity) {
                            const matched = TN_DISTRICTS.find(d => 
                                d.name.toLowerCase() === rawCity.toLowerCase() ||
                                rawCity.toLowerCase().includes(d.name.toLowerCase()) ||
                                d.name.toLowerCase().includes(rawCity.toLowerCase())
                            );
                            if (matched) {
                                detectedName = matched.name;
                                detectedCode = matched.code;
                            } else {
                                detectedName = rawCity;
                                detectedCode = rawCity.substring(0, 3).toUpperCase();
                            }
                        }
                    }
                } catch (e) {
                    console.warn('Geocoding API network issue, falling back to coordinate distance matching:', e);
                }

                // Fallback: If reverse geocode was not available, find the closest Tamil Nadu district by coordinates
                if (!detectedName) {
                    const closest = findClosestDistrict(lat, lon);
                    if (closest) {
                        detectedName = closest.name;
                        detectedCode = closest.code;
                    } else {
                        detectedName = 'Coimbatore';
                        detectedCode = 'CBE';
                    }
                }

                btn.innerHTML = `<i class="fa-solid fa-check"></i> ${detectedName}`;
                btn.classList.remove('border-custom-light', 'text-custom-dark', 'bg-custom-light/5');
                btn.classList.add('bg-green-500', 'text-white', 'border-green-500');

                // Persist the actual detected location
                saveGlobalLocation(detectedName, detectedCode);
                closeLocationSelector();
                
                setTimeout(() => {
                    btn.innerHTML = originalText;
                    btn.disabled = false;
                    btn.classList.remove('bg-green-500', 'text-white', 'border-green-500');
                    btn.classList.add('border-custom-light', 'text-custom-dark', 'bg-custom-light/5');
                    loadTurfs();
                }, 800);
            },
            (error) => {
                let msg = 'Unable to retrieve location.';
                if (error.code === error.PERMISSION_DENIED) {
                    msg = 'Location permission denied.';
                }
                const searchInput = document.getElementById('locationSelectorSearch');
                if (searchInput) {
                    searchInput.value = msg;
                    setTimeout(() => searchInput.value = '', 3000);
                }
                btn.innerHTML = originalText;
                btn.disabled = false;
            },
            { enableHighAccuracy: true, timeout: 8000 }
        );
    }

    function initCarousel() {
        const slides = document.querySelectorAll('.carousel-slide');
        const nextBtn = document.getElementById('nextBtn');
        const prevBtn = document.getElementById('prevBtn');
        let current = 0;
        let autoSlide;
        if (!slides.length) return;

        function showSlide(index) {
            slides.forEach((slide, i) => {
                if (i === index) { 
                    slide.classList.remove('opacity-0', 'pointer-events-none'); 
                    slide.classList.add('opacity-100', 'z-10', 'pointer-events-auto'); 
                } else { 
                    slide.classList.remove('opacity-100', 'z-10', 'pointer-events-auto'); 
                    slide.classList.add('opacity-0', 'pointer-events-none'); 
                }
            });
        }
        function nextSlide() { current = (current + 1) % slides.length; showSlide(current); }
        function prevSlide() { current = (current - 1 + slides.length) % slides.length; showSlide(current); }

        if (nextBtn && prevBtn) {
            nextBtn.addEventListener('click', () => { nextSlide(); restartAutoSlide(); });
            prevBtn.addEventListener('click', () => { prevSlide(); restartAutoSlide(); });
        }
        function restartAutoSlide() { clearInterval(autoSlide); autoSlide = setInterval(nextSlide, 5000); }
        showSlide(current);
        autoSlide = setInterval(nextSlide, 5000);
    }

    // ===== TURFS & BOOKING =====
    async function loadTurfs() {
        const container = document.getElementById('turfsContainer');
        if (!container) return;
        const loading = document.getElementById('turfsLoadingIndicator');
        const error = document.getElementById('turfsErrorBanner');
        loading?.classList.remove('hidden');
        loading?.classList.add('flex');
        error?.classList.add('hidden');
        container.innerHTML = '';
        try {
            const result = await EMS_CUSTOMER_TURF.listGrounds({ city: selectedDistrict, pageSize: 50 });
            if (!result.items.length) {
                container.innerHTML = '<div class="col-span-full py-12 text-center text-sm font-bold text-gray-400">No Turf venues are available in this location.</div>';
                return;
            }
            result.items.forEach(t => container.appendChild(renderTurfCard(t)));
        } catch (err) {
            if (error) {
                error.textContent = err.message || 'Unable to load Turf venues.';
                error.classList.remove('hidden');
            }
        } finally {
            loading?.classList.add('hidden');
            loading?.classList.remove('flex');
        }
    }

    function renderTurfCard(turf) {
        const fallbackImg = 'https://placehold.co/600x400/eee/999?text=Turf';
        const card = document.createElement('div');
        card.className = 'w-full group cursor-pointer card-zoom flex flex-col bg-white rounded-xl sm:rounded-3xl shadow-sm border border-gray-100 overflow-hidden hover:shadow-soft hover:border-custom-light/20 transition-all duration-300';
        card.innerHTML = `
            <div class="relative h-24 sm:h-44 overflow-hidden bg-gray-100 shrink-0">
                <img src="${fallbackImg}" alt="${escapeHtml(turf.name || 'Turf')}" class="card-zoom-image w-full h-full object-cover">
            </div>
            <div class="p-2 sm:p-5 flex flex-col flex-grow">
                <h3 class="text-[11px] sm:text-lg font-extrabold text-gray-900 leading-tight truncate mb-0.5 sm:mb-1">${turf.name}</h3>
                <p class="text-[9px] sm:text-xs text-gray-500 mb-1.5 sm:mb-3 font-semibold truncate"><i class="fa-solid fa-location-dot mr-1 text-custom-light"></i>${escapeHtml(turf.city || 'Location not provided')}</p>
                <div class="mt-auto pt-1.5 sm:pt-4 border-t border-gray-50 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-1.5 sm:gap-0">
                    <span class="text-[10px] sm:text-sm font-extrabold text-gray-900">Price shown with slots</span>
                    <button class="w-full sm:w-auto text-[10px] sm:text-xs font-bold bg-gray-50 border border-gray-100 text-gray-700 px-2 sm:px-4 py-1 sm:py-2 rounded-md sm:rounded-lg hover:bg-custom-light hover:text-white transition-all shadow-sm">Book</button>
                </div>
            </div>
        `;
        card.onclick = () => {
            openBookingPage(turf);
        };
        return card;
    }

    function openBookingPage(turf) {
        document.getElementById('bookingTurfName').innerText = turf.name;
        document.getElementById('bookingTurfLocation').innerHTML = '<i class="fa-solid fa-location-dot mr-2 text-custom-light"></i> ' + escapeHtml(turf.city || 'Location not provided');
        document.getElementById('bookingTurfPrice').innerText = '—';
        document.getElementById('bookingTurfImage').src = 'https://placehold.co/600x400/eee/999?text=Turf';
        currentBookingState = { ground: turf, groundId: turf.id, selectedDate: '', selectedSlots: [], slots: [], turfName: turf.name, turfImage: '' };
        generateDates();
        generateSlots();
        updateBookingSummary();
        navigateTo('booking');
    }

    function closeBookingPage() { navigateTo('home'); }

    function generateDates() {
        const container = document.getElementById('dateSelector');
        if (!container) return;
        container.innerHTML = '';
        const today = new Date();
        const days = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
        const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
        for (let i = 0; i < 7; i++) {
            const date = new Date(today);
            date.setDate(today.getDate() + i);
            const isSelected = i === 0;
            const activeClass = isSelected ? 'bg-custom-dark text-white shadow-md border-custom-dark' : 'bg-white text-gray-700 border-gray-100 hover:border-gray-300';
            const btn = document.createElement('button');
            const dateStr = date.getFullYear() + '-' + String(date.getMonth() + 1).padStart(2, '0') + '-' + String(date.getDate()).padStart(2, '0');
            btn.className = `flex-none flex flex-col items-center justify-center w-16 h-20 rounded-[18px] border-2 transition-all duration-200 ${activeClass} date-btn`;
            btn.dataset.date = dateStr;
            btn.innerHTML = `<span class="text-[10px] font-extrabold mb-1 opacity-80 uppercase tracking-wide">${days[date.getDay()]}</span><span class="text-xl font-extrabold">${date.getDate()}</span><span class="text-[9px] font-extrabold opacity-80 uppercase tracking-widest">${months[date.getMonth()]}</span>`;
            btn.onclick = () => {
                document.querySelectorAll('.date-btn').forEach(b => b.className = `flex-none flex flex-col items-center justify-center w-16 h-20 rounded-[18px] border-2 transition-all duration-200 bg-white text-gray-700 border-gray-100 hover:border-gray-300 date-btn`);
                btn.className = `flex-none flex flex-col items-center justify-center w-16 h-20 rounded-[18px] border-2 transition-all duration-200 bg-custom-dark text-white shadow-md border-custom-dark date-btn`;
                currentBookingState.selectedDate = btn.dataset.date;
                currentBookingState.selectedSlots = [];
                generateSlots();
                updateBookingSummary();
            };
            container.appendChild(btn);
            if (isSelected) currentBookingState.selectedDate = dateStr;
        }
    }

    async function generateSlots() {
        const container = document.getElementById('slotGrid');
        if (!container) return;
        container.innerHTML = '<div class="col-span-full flex items-center justify-center py-8"><div class="w-6 h-6 border-2 border-gray-100 border-t-custom-light rounded-full animate-spin"></div></div>';
        try {
            const availability = await EMS_CUSTOMER_TURF.getAvailability(currentBookingState.groundId, currentBookingState.selectedDate);
            currentBookingState.slots = availability.slots;
            container.innerHTML = '';
            if (!availability.slots.length) {
                container.innerHTML = '<div class="col-span-full py-8 text-center text-sm font-medium text-gray-400">No slots available for this date.</div>';
                return;
            }
            availability.slots.forEach(slot => {
                const btn = document.createElement('button');
                btn.innerText = slot.label;
                btn.dataset.time = slot.label;
                if (!slot.available || !Number.isFinite(slot.price)) {
                    btn.className = 'py-2.5 px-1 rounded-xl border-2 border-gray-50 bg-gray-50 text-gray-300 font-bold text-xs sm:text-sm cursor-not-allowed';
                    btn.disabled = true;
                } else {
                    btn.className = 'slot-btn py-2.5 px-1 rounded-xl border-2 border-gray-100 bg-white text-gray-700 font-extrabold text-xs sm:text-sm hover:border-custom-light hover:text-custom-light transition-all shadow-sm';
                    btn.onclick = () => toggleSlot(btn);
                }
                container.appendChild(btn);
            });
        } catch (err) {
            currentBookingState.slots = [];
            container.innerHTML = '<div class="col-span-full py-8 text-center text-sm font-bold text-red-500">' + escapeHtml(err.message || 'Unable to load Turf availability.') + '</div>';
        }
    }

    function toggleSlot(btn) {
        const time = btn.dataset.time;
        const index = currentBookingState.selectedSlots.findIndex(slot => slot.label === time);
        if (index > -1) { 
            currentBookingState.selectedSlots.splice(index, 1); 
            btn.classList.remove('bg-custom-light','text-white','border-custom-light','shadow-glow'); 
            btn.classList.add('bg-white','text-gray-700','border-gray-100'); 
        } else { 
            const selected = currentBookingState.slots.find(slot => slot.label === time && slot.available && Number.isFinite(slot.price));
            if (!selected) return;
            currentBookingState.selectedSlots.push(selected); 
            btn.classList.remove('bg-white','text-gray-700','border-gray-100'); 
            btn.classList.add('bg-custom-light','text-white','border-custom-light','shadow-glow'); 
        }
        updateBookingSummary();
    }

    function updateBookingSummary() {
        const count = currentBookingState.selectedSlots.length;
        const subtotal = currentBookingState.selectedSlots.reduce((sum, slot) => sum + slot.price, 0);
        const taxes = 0;
        const total = subtotal;
        
        currentPaymentTotal = total;
        
        document.getElementById('summarySlotCount').innerText = count;
        document.getElementById('summarySubtotal').innerText = subtotal.toLocaleString('en-IN');
        document.getElementById('summaryTaxes').innerText = taxes.toLocaleString('en-IN', {maximumFractionDigits: 0});
        document.getElementById('summaryTotal').innerText = total.toLocaleString('en-IN', {maximumFractionDigits: 0});
        
        const payBtn = document.getElementById('proceedToPayBtn');
        if (count > 0) {
            payBtn.disabled = false;
            payBtn.innerText = 'Create Booking · ₹' + total.toLocaleString('en-IN');
            payBtn.onclick = createHomeTurfBooking;
        } else { 
            payBtn.disabled = true; 
            payBtn.innerText = 'Select a Slot';
            payBtn.onclick = null; 
        }
    }

    async function createHomeTurfBooking() {
        if (!currentUser) {
            showAlert('Please log in before booking a Turf slot.', 'error');
            navigateTo('login');
            return;
        }
        const btn = document.getElementById('proceedToPayBtn');
        const original = btn.innerText;
        btn.disabled = true;
        btn.innerText = 'Creating booking...';
        try {
            const latest = await EMS_CUSTOMER_TURF.getAvailability(currentBookingState.groundId, currentBookingState.selectedDate);
            const labels = currentBookingState.selectedSlots.map(slot => slot.label);
            const confirmed = latest.slots.filter(slot => labels.includes(slot.label) && slot.available && Number.isFinite(slot.price));
            if (confirmed.length !== labels.length) {
                currentBookingState.selectedSlots = [];
                updateBookingSummary();
                await generateSlots();
                throw new Error('One or more selected slots are no longer available. Please select again.');
            }
            const booking = await EMS_CUSTOMER_TURF.createBooking({
                ground: currentBookingState.ground,
                date: currentBookingState.selectedDate,
                slots: confirmed,
                user: currentUser
            });
            window.alert('Booking ' + booking.id + ' was created with status ' + (booking.status || 'pending_payment') + '. Payment is not available from the current Turf backend, so this booking is not confirmed.');
        } catch (err) {
            window.alert(err.message || 'Turf booking could not be created.');
        } finally {
            btn.disabled = currentBookingState.selectedSlots.length === 0;
            btn.innerText = original;
        }
    }

    function processPayment() {
        window.alert('Turf payment is unavailable because the current backend cannot create or verify Turf payment orders.');
=======
    toast.classList.remove('hidden', 'translate-x-full');
    setTimeout(function() { toast.classList.add('translate-x-full'); }, 4000);
}

// ── City slug helper ───────────────────────────────────────────
function citySlug(name) {
    if (!name) return '';
    return name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
}

// ── Loading spinner ────────────────────────────────────────────
function renderLoading(text) {
    return '<div class="flex flex-col items-center justify-center py-20"><div class="w-10 h-10 border-4 border-gray-100 border-t-custom-light rounded-full animate-spin mb-4"></div><p class="text-gray-500 font-bold text-sm">' + (text || 'Loading...') + '</p></div>';
}

function renderEmpty(title, subtitle, action) {
    var html = '<div class="text-center py-20">' +
        '<div class="w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4 text-gray-400 text-3xl"><i class="fa-regular fa-calendar"></i></div>' +
        '<h3 class="text-lg font-extrabold text-gray-900 mb-2">' + title + '</h3>' +
        '<p class="text-sm text-gray-500 font-medium mb-6">' + subtitle + '</p>';
    if (action) html += '<button onclick="' + action + '" class="bg-custom-light text-white font-extrabold px-8 py-3 rounded-xl hover:bg-custom-lightHover transition-all shadow-glow">Browse</button>';
    html += '</div>';
    return html;
}

// ── Splash screen ──────────────────────────────────────────────
(function() {
    var splash = document.getElementById('splashScreen');
    if (splash) {
        setTimeout(function() {
            splash.style.opacity = '0';
            splash.style.transition = 'opacity 0.7s';
            setTimeout(function() { splash.style.display = 'none'; }, 700);
        }, 1500);
>>>>>>> d3846f2 (chore: finalize production frontend)
    }
    renderLocationGrid();
})();
</script>

<<<<<<< HEAD
    // ===== MY BOOKINGS =====
    async function renderMyBookings() {
        const list = document.getElementById('bookingsList');
        const empty = document.getElementById('noBookingsMsg');
        if (!list) return;
        if (!currentUser) {
            list.innerHTML = '';
            if (empty) empty.style.display = '';
            return;
        }
        list.innerHTML = '<div class="py-10 text-center text-sm font-bold text-gray-400">Loading Turf bookings...</div>';
        if (empty) empty.style.display = 'none';
        try {
            const response = await EMS_API.turf.getTurfBookings();
            if (!response.ok || !response.data || response.data.success !== true || !Array.isArray(response.data.data)) {
                throw new Error((response.data && (response.data.error || response.data.message)) || 'Unable to load Turf bookings.');
            }
            const bookings = response.data.data;
            if (bookings.length === 0) {
                list.innerHTML = '';
                if (empty) empty.style.display = '';
                return;
            }
            list.innerHTML = bookings.map((b) => `
            <div class="bg-white rounded-2xl p-5 sm:p-6 border border-gray-100 shadow-soft flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                <div class="flex items-center gap-4">
                    <div class="w-12 h-12 rounded-xl bg-custom-light/10 flex items-center justify-center text-custom-light text-xl shrink-0"><i class="fa-solid fa-futbol"></i></div>
                    <div>
                        <h4 class="font-extrabold text-sm text-gray-900">${escapeHtml(b.ground_name || 'Turf')}</h4>
                        <p class="text-xs text-gray-500 font-medium mt-0.5">${Array.isArray(b.slots) ? b.slots.length : (b.slot ? 1 : 0)} slot(s) &middot; ${escapeHtml(b.date || 'Date unavailable')}</p>
                        <p class="text-xs text-gray-400 font-semibold">${escapeHtml(Array.isArray(b.slots) ? b.slots.join(', ') : (b.slot || ''))}</p>
                    </div>
                </div>
                <div class="flex items-center gap-3 sm:gap-4">
                    <span class="text-lg font-extrabold text-gray-900">₹${Number(b.total_amount || b.amount || 0).toLocaleString('en-IN')}</span>
                    <span class="bg-amber-50 text-amber-700 text-[10px] font-extrabold px-2.5 py-1 rounded-full border border-amber-200">${escapeHtml(b.status || 'pending')}</span>
                </div>
            </div>
            `).join('');
        } catch (err) {
            list.innerHTML = '<div class="rounded-xl border border-red-200 bg-red-50 px-4 py-8 text-center text-sm font-bold text-red-700">' + escapeHtml(err.message || 'Unable to load Turf bookings.') + '</div>';
        }
    }

    // ===== SIDEBAR =====
    function toggleSidebar() {
        const sidebar = document.getElementById('mobileSidebar');
        const overlay = document.getElementById('sidebarOverlay');
        const isClosed = sidebar.classList.contains('translate-x-full');
        if (isClosed) { 
            sidebar.classList.remove('translate-x-full'); 
            overlay.classList.remove('hidden'); 
            setTimeout(() => overlay.classList.remove('opacity-0'), 10); 
        } else { 
            sidebar.classList.add('translate-x-full'); 
            overlay.classList.add('opacity-0'); 
            setTimeout(() => overlay.classList.add('hidden'), 300); 
        }
    }
    </script>
=======
>>>>>>> d3846f2 (chore: finalize production frontend)
</body>
</html>
